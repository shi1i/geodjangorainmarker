OpenStreetMap находится в localhost:[port]/api/home/
.../api/last_rainfall/<latitude>/<longitude> - связь с функций в views, она должна брать данные из БД и сопоставлять их с параметрами в <>, но это не работает(
.../admin - это админка, в ней можно быстро добавлять данные в БД по существующим классам, созданным в models при помощи makemigrtaions -> migrate
ogrinspect ... - может описать структуру БД из файла на языке python в виде класса
